type EmailArgs = record {
    to: text;
    subject: text;
    message: text;
    action_url: text;
};

type ConfigurationArgs = record {
    courier_url: text;
    courier_auth_token: text;
    template_id: text;
};

service: (ConfigurationArgs) -> {
    "send_email": (EmailArgs) -> (text);
}